#!/bin/sh
set -eu

usage() {
    printf '%s\n' \
        'usage: ./src/expect/build CC' \
        'usage: ./src/expect/build --clean' >&2
}

if [ "$#" -eq 1 ] && [ "$1" = '--clean' ]; then
    rm -f .bin/expect
    exit 0
fi

if [ "$#" -ne 1 ]; then
    usage
    exit 2
fi

cc=$1

mkdir -p .bin
"$cc" src/expect/main.c -o .bin/expect
./src/expect/test/main
