# dud

`dud` is in a policy-research pivot.

The active working context is the advisory research bundle at
[`for-agents/research/policy-research/`](for-agents/research/policy-research/).
That bundle contains captured copies of the former live docs, HTML policy
drafts, experiments, and the source-backed meta-policy report.

Live root docs are intentionally small while the policy report is being
hand-edited into a more actionable shape.
