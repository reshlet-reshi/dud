# Policy

Tracked policy runbooks live under `policy/`.

`punctuation-policy.md` is experimental while the runbook migration is in
progress.

`py.md` is the experimental Python quality policy.

`sh.md` is the experimental shell quality policy.

Until the cutover is proven, `.stash/policy/report-punctuation-policy.py`
remains the temporary parity oracle for the punctuation policy.
