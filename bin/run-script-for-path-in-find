#!/bin/sh
set -eu

usage() {
    printf '%s\n' \
        'usage: ./bin/run-script-for-path-in-find <runner> <script> <find-arg>...'
}

if [ "$#" -lt 3 ]; then
    usage >&2
    exit 2
fi

runner="$1"
script="$2"
shift 2

find . "$@" -exec "./bin/run-script-foreach" "$runner" "$script" {} +
