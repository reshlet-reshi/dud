#!/bin/sh
set -eu

usage() {
    printf '%s\n' \
        'usage: ./bin/run-script-foreach <runner> <script> <path>...'
}

if [ "$#" -lt 3 ]; then
    usage >&2
    exit 2
fi

runner="$1"
script="$2"
shift 2

status=0

for arg do
    "$runner" "$script" "$arg" || status=1
done

exit "$status"
