#!/bin/sh
set -eu

./bin/run-script-for-path-in-find "$1" "$2" \
    \( -path './.git' -o -path './.bin' -o -path './.vendor' \) -prune -o \
    -type f \
    \( \
        -name '*.sh' \
        -o \
        -exec ./bin/has-sh-shebang {} \; \
    \)
