#!/bin/sh
set -eu

./bin/cache-vendored

musl_cc='.cache/vendor/musl-cc/x86_64-linux-musl-native/x86_64-linux-musl-native/bin/x86_64-linux-musl-gcc'
out='.cache/test/return-0'

mkdir -p "$(dirname "$out")"
"$musl_cc" -static test/return-0.c -o "$out"
"$out"
