#!/bin/sh
set -eu

. ./lib/set-dud-tmp.sh
rm -rf "$dud_tmp"

self_check=false
for arg in "$@"; do
    if [ "$arg" = "--self-check" ]; then
        self_check=true
        break
    fi
done

if [ "$self_check" = true ]; then
    ./self-check/0000-fetch-musl-cc
fi
